const { https } = require('firebase-functions')

export const editPod = https.onRequest(async (data, context) => {})
export const removePod = https.onRequest(async (data, context) => {})
export const ratePod = https.onRequest(async (data, context) => {})
